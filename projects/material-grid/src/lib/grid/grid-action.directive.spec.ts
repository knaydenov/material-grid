import { GridActionDirective } from './grid-action.directive';

describe('GridActionDirective', () => {
  it('should create an instance', () => {
    const directive = new GridActionDirective();
    expect(directive).toBeTruthy();
  });
});
