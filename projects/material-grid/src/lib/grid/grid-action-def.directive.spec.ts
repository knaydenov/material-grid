import { GridActionDefDirective } from './grid-action-def.directive';

describe('GridActionDefDirective', () => {
  it('should create an instance', () => {
    const directive = new GridActionDefDirective();
    expect(directive).toBeTruthy();
  });
});
