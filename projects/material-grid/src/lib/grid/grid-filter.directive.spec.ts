import { GridFilterDirective } from './grid-filter.directive';

describe('GridFilterDirective', () => {
  it('should create an instance', () => {
    const directive = new GridFilterDirective(null);
    expect(directive).toBeTruthy();
  });
});
