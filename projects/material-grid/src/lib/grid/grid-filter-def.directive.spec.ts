import { GridFilterDefDirective } from './grid-filter-def.directive';

describe('GridFilterDefDirective', () => {
  it('should create an instance', () => {
    const directive = new GridFilterDefDirective();
    expect(directive).toBeTruthy();
  });
});
